# 灶囍手作訂購管理系統 — CHANGELOG

> 專案啟動：2026/06/16
> 最後更新：2026/06/22
> 版本目錄以 Google Sheet 版本目錄為正式來源，版號為歷史紀錄，不因開發時序追溯重編。

---

## v1.8 — 歷史訂單基礎功能（2026/06/22）

### 定義
**範圍：**
- 今日訂單與非當日訂單分流顯示
- 非當日訂單不再累積於今日主畫面
- 具備基礎日期分組／歷史區塊顯示

**不包含**（列為後續「歷史訂單功能加強」，不併入本版）：
- 日期區間篩選
- 進階搜尋
- 歷史統計
- 匯出報表

### 內容
- 後台新增「📋 歷史訂單」頁籤，已完成訂單（已出貨／自取完成／取消）由「全部」頁籤移出，避免累積影響今日訂單檢視
- 歷史訂單依日期自動分組顯示，各組標示日期與筆數

### 修改檔案
- `admin.html`

---

## v1.7.2 — 訂單編號重構（2026/06/22）

### 問題
`generateOrderId()` 原本透過讀取 Google Sheet 現有資料、取最大序號+1 的方式生成編號。即使在 LockService 鎖保護下，仍因 Google Sheet 試算表引擎「寫入後讀取」可能存在短暫非即時一致性，導致不同 `requestId` 的訂單在高頻請求下生成相同編號（測試重現：3筆不同訂單皆生成 `Z20260622001`）。

### 修正
改用 Script Properties 維護「每日序號計數器」（key: `ORDER_SEQ_yyyyMMdd`），生成編號時直接對計數器執行「讀取→遞增→寫回」，不再依賴 Sheet 讀取結果。

### 驗證
不同 `requestId` 幾乎同時送出，訂單編號依序遞增、無重複，驗證通過。

### 修改檔案
- `Code.gs`（`generateOrderId()`）

---

## v1.7.1 — 防重複送單機制（2026/06/22）

### 新增
- 前端每次開啟確認彈窗時產生唯一 `requestId`（時間戳+隨機字串）
- 後端以 Script Properties 暫存近期已處理的 `requestId`（有效期10分鐘，逾期自動視為可清除）
- 同一 `requestId` 重複送出時，直接回傳第一次的處理結果，不重複建立訂單

### 修正：競態條件（LockService）
- **問題**：初版機制存在競態條件——兩個幾乎同時抵達的相同 `requestId` 請求，可能同時通過「尚未處理」的檢查，各自完成寫入，產生重複訂單。經實際測試（同一 requestId 連續發送兩次請求）重現此問題
- **修正**：`submitOrder()` 整段邏輯（requestId檢查／重新計價／金額比對／寫入Sheet／標記完成）以 `LockService.getScriptLock()` 包覆，確保整個流程對同一時間的多個請求是序列化、不可被插隊的單一操作
- 修正後重新測試：同一 `requestId` 連續送出，Sheet 僅新增一筆訂單，驗證通過

### 修改檔案
- `index.html`（`openConfirm()` 產生requestId）
- `Code.gs`（`checkDuplicateRequest()`／`markRequestProcessed()`，`submitOrder()` 加上LockService）

---

## v1.7 — 後端計價與送單驗證（2026/06/22）

### 背景
原本前台計算金額（商品小計、運費、總金額）並直接送往後端寫入 Google Sheet，後端完全信任前端數字，零驗證。本版將「計算金額」的權威來源從前端轉移至後端，前端僅送出結構化商品清單與配送選擇，最終金額一律由後端依 Google Sheet 商品主檔重新計算。

### 新增：後端權威計價
- 新增 `getProductCatalog()`：以「商品設定」Sheet 為唯一權威來源，讀取商品名稱／單價／重量／上架狀態
- 新增後端版 `SET_DECOMPOSE_BACKEND` 套餐拆解對照表（與前端版本保持同步，寫死於 Code.gs 常數）
- 新增 `calculateOrderPricing()`：依結構化商品清單（商品ID+數量）與取貨方式，重新計算商品小計、總重量、配送件數、運費、總金額、品項明細文字
- 前端新增 `getOrderItemsList()`，將購物車內容組成結構化陣列 `[{id, qty}]` 隨訂單送出，取代原本僅送純文字 `itemDetail` 的方式

### 新增：金額驗證機制（PRICE_MISMATCH）
- 前端送出 `expectedTotal`（前端預期金額，僅作比對用途，非寫入依據）
- 後端比對 `expectedTotal` 與後端計算結果：
  - 一致 → 正常寫入訂單，使用後端計算的 `total`／`shippingFee`／`itemDetail`（不採用前端送來的版本）
  - 不一致 → 不寫入 Sheet，回傳固定錯誤代碼 `PRICE_MISMATCH` 與正確金額，前端提示客人「金額已更新，請重新確認」，產生新 `requestId` 後才能重新送出
  - 設計原則：金額異動絕不靜默覆蓋，必須經客人確認後才送出，避免信任問題
- 新增 `INVALID_ITEMS` 防呆：訂單內含不存在於商品主檔、未上架、或數量不合法的商品時，整筆拒絕，不寫入

### 確認不啟用（留待庫存管理階段）
- `deductStock()` 本版刻意不啟用，維持既有狀態（已定義但不於 `submitOrder` 中呼叫）。原因：扣庫存與「庫存是否足夠」檢查須為同一原子操作的兩部分，本版僅完成計價驗證，缺少庫存量檢查時啟用扣庫存有導致庫存變負數的風險

### 修改檔案
- `index.html`（`submitOrder()`、新增 `getOrderItemsList()`）
- `Code.gs`（新增 `getProductCatalog()`／`calculateOrderPricing()`，重構 `submitOrder()`）

### 測試結果摘要（v1.7／v1.7.1／v1.7.2 合併測試）
| 類別 | 項目 | 結果 |
|---|---|---|
| 核心安全 | 竄改 `expectedTotal` | ✅ 正確攔截為 `PRICE_MISMATCH`，未寫入 |
| 核心安全 | 同 `requestId` 重複送出 | ✅ 僅產生一筆訂單 |
| 核心安全 | 不同 `requestId` 幾乎同時送出 | ✅ 訂單編號正確遞增、無重複 |
| 功能 | 正常下單（套餐+單品混合） | ✅ 金額／重量／品項明細皆正確 |
| 功能 | 無效商品ID | ✅ 正確攔截為 `INVALID_ITEMS` |
| 功能 | 自取／宅配／7-11 | ✅ 運費計算皆正確（$0／件數×$200／件數×$150） |
| 回歸 | v1.7.0、v1.6.5、v1.6.6 既有功能 | ✅ 全數通過，未受影響 |

---

## v1.6.6 — 安全強化修正（2026/06/21）

### 背景
v1.6.5 完成後經複查，提出四項必修安全項目。

### 修正內容
1. **XSS 風險修復**：`esc()` 函式原僅轉義 `& < >`，補上 `" '` 轉義；補上原本遺漏 `esc()` 的電話欄位（客人可控輸入，原為儲存型XSS風險點）；`orderId` 拼接於HTML屬性處與歷史訂單日期標籤一併補強防禦性轉義
2. **Token 不放 URL／不輸出 console**：`getOrders` 由 GET 改為 POST，token 改置於 request body；移除所有會印出完整URL、token、或訂單明細內容的 `console.log`
3. **固定錯誤代碼**：所有失敗回應統一新增 `code` 欄位（`AUTH_REQUIRED`／`AUTH_EXPIRED`／`AUTH_REPLACED`／`LOGIN_FAILED`／`VALIDATION_FAILED`／`ORDER_NOT_FOUND`／`UNKNOWN_ACTION`／`SERVER_ERROR` 等），前端改用 `isAuthError(code)` 判斷，取代原本不可靠的中文字串比對
4. **updateOrder 雙重白名單**：新增 `UPDATE_FIELD_WHITELIST`，同時限制「可修改欄位」與「該欄位可接受的值」，杜絕任意字串寫入 Google Sheet

### 修改檔案
- `admin.html`（登入流程、esc強化）
- `Code.gs`（`doGet`／`doPost`／`updateOrder` 白名單）

---

## v1.6.5 — 後台登入驗證（2026/06/21）

### 背景
`admin.html` 原僅以前端密碼提示（prompt）作為保護，`getOrders`／`updateOrder` 後端完全不驗證身分，任何人知道 API 網址即可繞過前端取得全部客人個資或竄改訂單狀態。

### 新增：Token 登入驗證機制
- 密碼存於 Google Apps Script「Script Properties」（`ADMIN_PASSWORD`），不寫死於任何前端檔案
- 新增 `login()`／`verifyToken()`／`logout()`：登入成功產生隨機 token（兩組UUID拼接），24小時內有效，過期判斷一律以後端時間為準；同帳號新登入會使舊 token 立即失效
- `getOrders`、`updateOrder` 皆須通過 `verifyToken()` 驗證；未通過則直接拒絕，`getOrders` 失敗時**不回傳任何訂單陣列**（姓名、電話、地址等個資完全不外洩）

### 修改檔案
- `admin.html`（登入流程、token存取、API呼叫改帶token）
- `Code.gs`（新增 `login`／`verifyToken`／`logout`，`doGet`／`doPost`、`updateOrder` 加上驗證）

### 確認不影響
- 訂單資料結構、前台 `index.html` 完全不受影響

---

## v1.7.0 — 第一階段：既有 Bug 修復（2026/06/21）

> 備註：本版號開發時序早於 v1.6.5／v1.6.6（先完成既有Bug修復，後進行登入保護開發），
> 版號數字與開發時序不完全一致，依 Google Sheet 版本目錄原則維持版號不重編，特此註明時序。

### 背景
系統上線運作一段時間後，累積七項待修正問題，涵蓋顯示錯誤、統計邏輯、資料完整性與防呆機制。

### 修復清單
1. **付款後五碼顯示多餘符號**：`admin.html` 字串拼接打字錯誤，移除多餘 `+`
2. **今日訂單金額邏輯**：原始版本未排除取消訂單、且非實收金額（含未收款訂單）。修正為僅計入 `payStatus` 為「已付款」或「已收現金」的訂單，並更名「今日實收金額」以名實相符（v1.5.5 當時新增此功能時名稱為「今日訂單金額」，本次修正調整邏輯與名稱，v1.5.5 歷史紀錄不予追溯改寫）
3. **後台狀態更新失敗復原機制**：原本 API 失敗時畫面仍顯示已更新成功的假象。修正為失敗時將畫面與本地資料還原至更新前狀態，並提示「已還原」
4. **配達日期下限**：新增 `min` 屬性與 `validate()` 雙重防護，禁止選擇早於今天的配達日期
5. **不可取日載入失敗警告**：原失敗時默默視為「全部可選」。修正為顯示明確警告，提醒客人聯絡店家確認，不阻擋下單（不可取日為店家排程考量，非強制流程卡點）
6. **商品重量資料載入失敗時阻擋宅配/7-11送單**：原失敗時重量歸零，導致運費被誤算為 $0。修正為失敗時阻擋宅配／7-11送單（避免免費出貨），自取不受影響（運費恆為0，不依賴重量資料）
7. **`getOrders()` 時間格式不一致**：Google Sheet 時間欄位視儲存狀態可能輸出 ISO 格式或斜線格式，導致前端 `String(o.time).startsWith(ymd)` 比對失效，影響今日訂單數／今日實收金額／今日自取宅配7-11統計／今日待處理區塊／歷史訂單日期分組共5處邏輯。修正為後端新增 `formatDateField()` 統一輸出格式

### 修改檔案
- `admin.html`（Bug 1、2、3）
- `index.html`（Bug 4、5、6）
- `Code.gs`（Bug 7）

### 確認不影響
- 全數為顯示/驗證/讀取邏輯調整，未變更 Google Sheet 任何欄位順序或既有資料

---

## v1.6.4 — 前台購物車明細優化（2026/06/20）

### 調整內容
購物籃商品區整併至金額明細區，預設僅顯示精簡摘要，需要時可展開查看商品內容，降低客戶填寫訂購資料時的資訊負擔。

- 購物籃商品清單整併進金額明細展開區，採單層收合（點「🛒 已選商品（N項）」直接展開/收合，不需多層點擊）
- 前台隱藏總重量、件數顯示（僅保留於背景運算，供運費計算使用）
- 明細區保留：商品金額、配送方式、運費、訂單總計
- 超重情況僅顯示「因超過單件重量限制，已自動計算分件運費」，不顯示總重量、實際件數、分件計算公式

### 確認未修改
- 確認訂單 Modal 內容
- 訂單送出流程
- 運費計算邏輯（`calcShipping()`、`getOrderTotalWeight()`）

### 修改檔案
- `index.html`

---

## v1.6.3 — 重量資料流與運費規則改版（2026/06/19）

### 背景
原運費規則為固定費率（宅配$200／7-11 $150，滿$3,000免運），未考慮商品實際重量，與宅配通、7-11實際計費邏輯（依重量分件計費）不符。本版建立重量資料流並改版運費規則。

### 新增：重量資料流
- Google Sheet「商品設定」分頁新增 J 欄「重量(kg)」，僅單點商品（東坡肉/醬燒蹄膀/醬燒豬腳/香滷嫩筍）填入數字，套餐不另設重量欄位
- `Code.gs` `getProducts()` 新增 `weight` 欄位回傳
- `index.html` 新增：
  - `SET_DECOMPOSE` 套餐拆解對照表（沿用 admin.html 既有邏輯）
  - `loadProductWeights()` 載入單品重量，失敗時安全降級為 0，不影響下單流程
  - `getItemWeight(key)` 查詢單品或套餐重量（套餐依組成單品加總，不另設套餐重量欄位）
  - `getOrderTotalWeight()` 計算整張訂單總重量

### 新增：運費規則改版
- 件數計算：`件數 = ceil(總重量 / 單件重量上限)`，總重量為 0 時件數視為 0（不產生運費）
- 宅配通低溫宅配：單件限 20kg，$200／件
- 7-11 店到店：單件限 10kg，$150／件
- 訂單金額（不含運費）未滿 $3,000：運費 = 件數 × 單件運費
- 訂單金額（不含運費）滿 $3,000：第一件免運，超出件數 × 單件運費
- 自取：運費恆為 0

### 新增：運費規則前台顯示
為避免客戶對「滿$3,000為何仍收運費」產生疑問，於以下兩處顯示運費規則說明：
- 取貨方式選擇區下方（選擇宅配/7-11店到店時顯示對應規則）
- 購物車運費明細展開區（與試算金額並列顯示）
- 首頁固定「運費說明」區塊文字格式統一：標題（醒目）／金額與限制（紅字加粗）／補充說明（小字），「超過需寄第二件」統一改為「超過需加寄一件」

### 修改檔案
- Google Sheet「商品設定」分頁（新增 J 欄）
- `Code.gs`（`getProducts()`）
- `index.html`

---

## v1.6.2 — 後台UI優化／手機版（2026/06/19）

### 調整內容
- 後台統計卡分主次：第一區（待出貨／未收款／今日營收／今日訂單）優先顯示；第二區（今日自取／今日宅配／今日7-11／總訂單數）手機版預設收合，桌機版維持展開
- 空狀態（無訂單）高度優化，大幅縮減留白，減少手機版滑動距離

### 確認未修改
- 資料結構、Google Sheet、功能邏輯不變，純前端顯示調整

### 修改檔案
- `admin.html`

---

## v1.6.1 — 修復 v1.6 升版時遺失的希望配達日期功能（2026/06/19）

### 性質說明
本次為**修復**，非新功能開發。v1.6 升版過程中，因誤用舊版 index.html 為基底，
導致 v1.5.5 已完成的「希望配達日期」前台功能遺失，本版修復此問題。

### 問題
v1.6 開發「不可取日控制」時，誤使用缺少「希望配達日期」功能的舊版 `index.html` 作為基底，導致：
- ❌ 希望配達日期輸入欄位消失
- ❌ 配達日期寫入 payload 消失
- ❌ 前台確認摘要顯示消失

### 確認未受影響
- ✅ `admin.html` 保留配達日期顯示程式
- ✅ `Code.gs` 保留 V欄寫入邏輯
- ✅ Google Sheet 欄位結構未變更

### 開發原則
以 v1.6 為主版本，保留：
- 前台：不可取日控制
- 後台：今日營收統計

補回 v1.5.5 功能（index.html）：
- ✅ 希望配達日期輸入欄位
- ✅ 顯示／隱藏邏輯（宅配顯示／7-11店到店顯示／自取隱藏）
- ✅ 訂單確認摘要顯示
- ✅ payload 寫入 Google Sheet V欄

### 修改檔案
- `index.html`

### 測試項目

**前台**
- [x] 自取時不顯示配達日期
- [x] 宅配時顯示配達日期
- [x] 7-11店到店顯示配達日期
- [x] 不可取日無法選取

**Google Sheet**
- [x] V欄成功寫入配達日期

**後台**
- [x] 訂單卡片顯示配達日期
- [x] 今日營收統計正常

**整體驗證**
- [x] 配達日期功能正常（已於正式網址截圖確認）
- [x] 舊功能無回歸問題（diff 比對僅新增10行，無刪除）
- [x] 不可取日控制正常
- [x] 今日營收統計正常運作於真實訂單

### 經驗記錄
這是「develop/main 分支不同步」問題的變形——本次根因不是分支沒合併，而是**新版本開發時引用了舊版檔案做基礎，導致已完成功能被覆蓋遺失**。建議之後每次升版前，用關鍵字比對新舊版本，確認沒有不小心漏掉既有功能。

---

## v1.6 — 不可取日控制完成（2026/06/19）

### 新增
- 不可取日控制（後台可設定不可取貨日期，前台自取日期選擇自動排除）
- 訂單搜尋
- 今日營收統計（含千分位格式）
- 今日7-11統計
- 後台統計卡順序調整：
  - 第一排：今日訂單／待出貨／未收款／今日營收
  - 第二排：今日自取／今日宅配／今日7-11／總訂單數

### 此版本發生的問題（已於 v1.6.1 修正）
開發不可取日控制時，誤使用缺少「希望配達日期」功能的舊版 index.html 作為基底，
導致 v1.5.5 已完成的配達日期前台功能（輸入欄位、payload寫入、確認摘要顯示）消失。
詳見 v1.6.1。

### 修正
- 地址填寫流程
- 運費規則
- 手機操作問題

### 改善
- 後台閱讀體驗
- 訂單管理流程

### 修改檔案
- `index.html`
- `admin.html`
- `Code.gs`（新增不可取日 API：`getBlockedDates`、`BLOCKED_SHEET`）

---

## v1.5.5 — 配達日期 & 後台統計優化（2026/06/19）

> ⚠️ **版號說明**：本 Release 實際同時包含「希望配達日期」與「後台統計金額」兩項功能，
> 兩者打包在同一個 GitHub Release（tag: v1.5.5）中發布，並非分屬 v1.5.4 / v1.5.5 兩個獨立版本。
> 若先前文件或對話中出現「v1.5.4＝配達日期」的版號對應，應以本記錄（GitHub Release 為準）修正之。

### 新增功能：希望配達日期
- 宅配/7-11訂單新增「希望配達日期」欄位（選填）
- 希望配達日期寫入 Google Sheet V欄
- 後台訂單卡片顯示希望配達日期
- 訂單確認摘要顯示配達日期

### 新增功能：後台統計金額
- 新增「今日營收」統計（含千分位格式）
- 新增「今日7-11」統計
- 統計卡順序調整：
  - 第一排：今日訂單／待出貨／未收款／今日營收
  - 第二排：今日自取／今日宅配／今日7-11／總訂單數

### 修改檔案
- `index.html`
- `admin.html`
- `Code.gs`（新增 V欄、getOrders 改讀 22 欄）

### 已知問題（已於 v1.6.1 修正）
此版本曾因 GitHub 上傳流程問題，正確檔案未能即時同步至 `main` 分支，且後續 v1.6 開發誤用舊版檔案，導致配達日期前台功能一度遺失。詳見 v1.6.1。

---

## v1.4 — 地址優化（2026/06/19）

### 新增
- 縣市下拉選單（含全台縣市）
- 鄉鎮市區下拉（依縣市連動）
- 詳細地址文字輸入
- 完整地址自動組合（縣市＋鄉鎮市區＋詳細地址）

### Google Sheet
- H欄：縣市
- I欄：鄉鎮市區
- J欄：詳細地址
- K欄：完整地址（自動組合）

### 修改檔案
- `index.html`
- `admin.html`

---

## v1.3.1 — 單點商品圖片（2026/06/18）

### 新增
- 單點商品卡片新增 72×72 圓角圖片區塊
- 四個品項圖片：東坡肉、醬燒蹄膀、醬燒豬腳、香滷嫩筍
- 圖片載入失敗時自動顯示 emoji 替代圖
- `PRODUCT_IMAGES` 設定區（JS最上方，方便日後更換圖片網址）
- 圖片存放於 GitHub repo（`/dongpo.jpg`、`/tipang.jpg`、`/pigjiao.jpg`、`/sun.jpg`）

### 修改檔案
- `index.html`

---

## v1.2 — 備貨統計（2026/06/18）

### 新增
- 後台底部新增「📦 備貨統計」區塊
- 支援切換：今日 / 明日 / 指定日期
- 自動拆解套餐，計算各品項備貨份數
- 排除已取消、已出貨、自取完成的訂單
- 以大字紅色顯示各品項需求數量

### 邏輯說明
- 自取訂單依 `pickupDate` 判斷日期
- 宅配/7-11訂單依訂購時間判斷日期

### 修改檔案
- `admin.html`

---

## v1.1 — 自取時段優化（2026/06/18）

### 新增
- 自取時段下拉選單（10個固定時段）
- 「其他時間」選項，顯示 `input type="time"` 自由填寫
- 其他時間顯示「⚠️ 需等店家確認」提醒
- 後台訂單卡片顯示自取日期＋時段
- 其他時間標註「⚠️ 需店家確認」

### 驗證規則
- 選自取時：日期必填、時段必填
- 選其他時間：時間欄位必填

### Google Sheet 欄位
- 自取時段（N欄）
- 其他自取時間（O欄）

### 修改檔案
- `index.html`
- `admin.html`

---

## v1.0.1 — 手機號碼驗證（2026/06/18）

### 修改
- 電話欄位新增驗證：必填、09開頭、共10碼
- 正則：`/^09\d{8}$/`
- placeholder 更新為「請輸入手機號碼（09開頭，共10碼）」
- maxlength 限制為 10

### 修改檔案
- `index.html`

---

## v1.0 — 正式可用版（2026/06/17）

### 前台功能
- 套餐組（A~H）＋單點區（東坡肉、蹄膀、豬腳、嫩筍）
- 取貨方式：自取 / 低溫宅配 / 7-11店到店
- 付款方式：現金（自取）/ 匯款（全部）
- 匯款資訊顯示（合作金庫 006）
- 宅配/7-11 鎖定匯款
- 運費自動計算（$3000免運、宅配$200、7-11 $150）
- 訂單確認摘要（送出前顯示）
- 送出成功顯示訂單編號

### 後台功能
- 訂單列表（依取貨方式分區）
- 篩選頁籤（今日/宅配/7-11/自取/待付款/已完成）
- 付款狀態修改（待確認/已付款/現金未收/已收現金）
- 出貨狀態修改（待處理/待出貨/已出貨/待自取/自取完成/取消）
- 已出貨/自取完成 → 自動移到「已完成」
- 統計數字（今日訂單/待確認/待出貨/總計）
- 每60秒自動重整
- 後台簡易密碼保護（`zaoshi2026`）

### Google Sheet
- 訂單分頁（21欄）
- 商品設定分頁
- 系統設定分頁
- 訂單編號格式：Z+日期+序號（例：Z20260618001）

### 技術
- 修正訂單編號數字/字串型別不符問題（`String()` 強制轉型）
- Apps Script 重新部署（URL：`AKfycbxR...`）
- GitHub Pages 部署（mumuworks/zaoshi-order）
- LOGO 去背處理（白底→透明PNG）

### 修改檔案
- `index.html`
- `admin.html`
- `Code.gs`

---

## 開發過程重要決策記錄

### 技術選型
- **為何選 Google Apps Script**：免費、零後端成本、與 Google Sheet 原生整合
- **為何選 GitHub Pages**：免費靜態部署、版本控制整合
- **為何不用資料庫**：店家不具備技術背景，Google Sheet 最易維護

### 已解決的關鍵問題

| 問題 | 原因 | 解法 |
|------|------|------|
| fetch 失敗（Failed to fetch）| Apps Script URL 舊版失效 | 重新部署取得新URL |
| 訂單找不到（找不到訂單錯誤）| Sheet 將訂單編號存為數字，比對字串失敗 | 訂單編號改為 `Z` 開頭（字母開頭不會被存為數字）|
| LOGO 不顯示 | JPG白底在紅色背景上整張變白 | Python去背轉透明PNG |
| Google Drive圖片無法顯示 | CORS跨來源請求被擋 | 改存於GitHub repo |
| main/develop分支不同步 | 一直在develop提交未合併main | 手動merge develop→main |

### 版本控制
- 工具：GitHub Desktop + GitHub Releases
- 分支策略：`develop`（開發）→ `main`（正式）
- Release 標記方式：GitHub Releases（因本機無 git 指令）
